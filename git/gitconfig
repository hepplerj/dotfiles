[hub]
    protocol = ssh

[user]
	name = Jason Heppler
	email = jason.heppler@gmail.com
	signingkey = 2EFE62637F1156B1

[github]
    user = hepplerj

[alias]
    co = checkout
    count = !git shortlog -sn
    praise = blame

[color]
    diff = auto
    status = auto
    branch = auto
    ui = true

[core]
    editor = vim
    excludesfile = ~/.gitignore
    autocrlf = false
    eol = lf

[apply]
    whitespace = nowarn

[mergetool]
    keepBackup = false

[difftool]
    prompt = false

[help]
    autocorrect = 1
[merge]
    tool = vimdiff

[commit]
    gpgsign = false

[pull]
    ff = only
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
