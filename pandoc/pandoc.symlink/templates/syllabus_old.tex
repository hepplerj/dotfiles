\documentclass[11pt]{article}
\usepackage{fullpage,fancyhdr}
%% color for the links 
\usepackage[usenames,dvipsnames]{color}

%% hyperlinks
\usepackage[
  colorlinks=true,
  urlcolor=MidnightBlue,
  plainpages=false,
    ]{hyperref}% color for the links 
\usepackage{graphicx}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
\setlength{\emergencystretch}{3em}  % prevent overfull lines
$if(numbersections)$
$else$
\setcounter{secnumdepth}{0}
$endif$
$if(verbatim-in-note)$
\VerbatimFootnotes % allows verbatim text in footnotes
$endif$
$for(header-includes)$
$header-includes$
$endfor$

\renewcommand{\headrulewidth}{0pt}
\fancyhead[C]{\includegraphics[width=2cm]{/Users/jheppler/Pictures/stanfordlogo.jpg}}
\fancyhead[L]{}
\fancyhead[R]{}
\pagestyle{plain}

$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(date)$
\date{$date$}
$endif$

\begin{document}
$if(title)$
\maketitle
$endif$
\thispagestyle{fancy} 
$for(include-before)$
$include-before$
$endfor$

\begin{flushleft}
$body$
\end{flushleft}

$for(include-after)$
$include-after$
$endfor$
\end{document}